// types.ts

export type AwardType = {
    league: string;
    prize: string;
};

export type InfoCardType = {
    icon: string;
    paragraph: string;
};

export type RequirementType = {
    icon: string,
    paragraph: string,
}
